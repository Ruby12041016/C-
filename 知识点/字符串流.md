# 字符串流（`stringstream`）整理笔记

在 C++ 中，**字符串流**就是：  
👉 把**字符串当作输入输出流来用**，像 `cin / cout` 一样操作字符串。

需要头文件：

```cpp
#include <sstream>
````

---

## 📌 一句话理解

> **字符串流 = 用流的方式处理字符串，实现字符串 ↔ 各种类型的转换与解析。**

---

## 📦 字符串流的三种类型

| 类名              | 作用    |
| --------------- | ----- |
| `stringstream`  | 可读可写  |
| `istringstream` | 只读字符串 |
| `ostringstream` | 只写字符串 |

---

## 🎯 主要用途

* ✅ 字符串转数字
* ✅ 数字转字符串
* ✅ 拆分字符串
* ✅ 模拟 `cin` 解析一行输入
* ✅ 拼接复杂字符串

---

## ✅ 基本用法

---

### ✅ 1. 字符串 → 数字

```cpp
string s = "12 3.14";
stringstream ss(s);

int a;
double b;

ss >> a >> b;
```

---

### ✅ 2. 数字 → 字符串

```cpp
int a = 10;
double b = 2.5;

stringstream ss;
ss << a << " " << b;

string s = ss.str();
```

---

### ✅ 3. 拆分字符串

```cpp
string line = "Tom 18 99.5";
stringstream ss(line);

string name;
int age;
double score;

ss >> name >> age >> score;
```

---

### ✅ 4. 按单词读取

```cpp
string line;
getline(cin, line);

stringstream ss(line);
string word;

while (ss >> word) {
    cout << word << endl;
}
```

---

## ✅ 专用流

### `istringstream`

```cpp
string s = "10 20";
istringstream iss(s);

int a, b;
iss >> a >> b;
```

---

### `ostringstream`

```cpp
ostringstream oss;
oss << 100 << " hello";

string s = oss.str();
```

---

## ⚠️ 常见注意点

---

### ✅ 1. 复用要清空

```cpp
ss.clear();
ss.str("");
```

---

### ✅ 2. 判断是否读取成功

```cpp
if (ss >> x) {
    // success
}
```

---

## 🧠 对比 `stoi`

| 方法             | 特点        |
| -------------- | --------- |
| `stoi`         | 转单个值      |
| `stringstream` | 可解析多个值    |
| `stringstream` | 可模拟 `cin` |
| `stringstream` | 适合复杂输入    |

---

## 🎯 适用场景

* 📌 ACM / 算法题
* 📌 解析一整行数据
* 📌 配置文件处理
* 📌 日志拼接
* 📌 字符串格式化

---

## 🧪 小例题

输入：

```
10 20 30
```

代码：

```cpp
string line;
getline(cin, line);

stringstream ss(line);
int x, sum = 0;

while (ss >> x) sum += x;

cout << sum;
```

---

## 💎 核心总结

> **`stringstream` 就是用 `cin / cout` 的方式处理字符串，让字符串和各种类型自由转换。**

---


